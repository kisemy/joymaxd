<%= form_with(model: item_ledger, local: true) do |form| %>
  <% if item_ledger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item_ledger.errors.count, "error") %> prohibited this item_ledger from being saved:</h2>

      <ul>
        <% item_ledger.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_no %>
    <%= form.text_field :item_no %>
  </div>

  <div class="field">
    <%= form.label :posting_date %>
    <%= form.date_select :posting_date %>
  </div>

  <div class="field">
    <%= form.label :document_no %>
    <%= form.text_field :document_no %>
  </div>

  <div class="field">
    <%= form.label :source_no %>
    <%= form.text_field :source_no %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
