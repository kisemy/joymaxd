<%= form_with(model: charge_type, local: true) do |form| %>
  <% if charge_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(charge_type.errors.count, "error") %> prohibited this charge_type from being saved:</h2>

      <ul>
        <% charge_type.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
