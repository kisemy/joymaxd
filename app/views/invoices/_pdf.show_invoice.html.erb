


<%= link_to 'Download PDF', invoice_path(@invoice, format: :pdf), class: 'btn btn-primary', id: 'pdf-download' %>

<script>
  document.getElementById('pdf-download').addEventListener('click', function(event) {
    event.preventDefault(); // Prevent default link behavior
    window.open(this.href, '_blank'); // Open PDF in a new tab
  });
</script>
  

 <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Invoice</title>
  <%= stylesheet_link_tag 'invoices', media: 'all' %>
</head>
<body>
  <div class="a4-container">
    <!-- Header with Logo -->
    <div class="header">
      <%= image_tag 'safaricomlogo.png', alt: 'Company Logo', class: 'logo' %>
      <h1>Invoice</h1>
    </div>

    <!-- Invoice Details -->
    <div class="invoice-details">
      <p><strong>Invoice Number:</strong> <%= @invoice.id %></p>
      <p><strong>Date:</strong> </p>
      <p><strong>Due Date:</strong> </p>
    </div>

    <!-- Billing and Shipping Information -->
    <div class="row">
      <div class="column">
        <h3>Billing Address</h3>
       
      </div>
      <div class="column">
        <h3>Shipping Address</h3>
     
      </div>
    </div>

    <h1>Invoice</h1>
  <p><strong>Invoice Number:</strong> <%= @invoice.id %></p>
  <p><strong>Date:</strong> <%= @invoice.date %></p>
  <p><strong>Customer:</strong> <%= @invoice.customer_name %></p>

  <table>
    <thead>
      <tr>
        <th>Product</th>
        <th>Quantity</th>
        <th>Price</th>
        <th>Total</th>
      </tr>
    </thead>
    <tbody>
      <% @invoice.invoice_lines.each do |line| %>
        <tr>
          <td><%= line.description %></td>
          <td><%= line.quantity %></td>
          <td><%= line.unit_price %></td>
          <td><%= line.amount %></td>
          
        </tr>
      <% end %>
    </tbody>
  </table>

    <!-- Footer -->
    <div class="footer">
      <p>Thank you for your business!</p>
    </div>
  </div>
</body>
</html>
